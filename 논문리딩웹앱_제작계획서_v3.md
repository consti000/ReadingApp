# 통합 논문 리딩·연구 웹앱 제작 계획서 (개인용 로컬 버전)

**프로젝트 가칭:** ReadLink (가칭)
**작성일:** 2026년 8월
**문서 목적:** MarginNote와 LiquidText의 핵심 기능을 통합한, 개인용 로컬 저장 기반 논문/문헌 리딩 웹앱 개발 계획 수립
**지원 플랫폼:** PC(Windows/Mac/Linux) 브라우저 + 안드로이드 태블릿(갤럭시탭 등) — 크로스 플랫폼 웹앱 전제

---

## 1. 프로젝트 개요

### 1.1 배경
대학원생과 연구자들은 논문을 읽고 정리하는 과정에서 서로 다른 강점을 가진 두 앱을 병행 사용하는 경우가 많다.

- **MarginNote**: 하이라이트 → 마인드맵 → 플래시카드로 자동 구조화, 암기·시험 대비에 강함
- **LiquidText**: 발췌 기반 자유 워크스페이스, 여러 문서 비교·근거 연결에 강함

본 프로젝트는 두 앱의 핵심 기능을 하나의 **웹 기반 앱**으로 통합하되, **개인 용도로만 사용**하는 것을 전제로 한다. 따라서 서버 인프라, 클라우드 동기화, 다중 사용자 계정, 결제 시스템 등은 배제하고, **모든 데이터를 사용자의 로컬 환경에 저장**하는 것을 기본 원칙으로 한다.

### 1.2 목표
- 하나의 발췌(excerpt)가 하이라이트·워크스페이스 카드·마인드맵 노드·플래시카드로 동시에 존재하는 통합 데이터 모델 구현
- 자유 배치형 워크스페이스(LiquidText식)와 자동 구조화형 마인드맵(MarginNote식)을 모두 지원
- 브라우저(또는 데스크톱 앱 형태)에서 동작하되, **모든 데이터는 로컬에만 저장**하여 외부 서버 의존성 없이 사용
- 별도 로그인/계정 없이 바로 사용 가능한 개인 도구로 설계
- **PC뿐 아니라 안드로이드 태블릿(갤럭시탭)에서도 동일하게 실행 가능**하도록, 저장 방식과 필기 입력 방식을 처음부터 크로스 플랫폼 호환 기준으로 설계
- S펜 등 스타일러스 압력 감지를 활용해 태블릿에서의 필기 경험을 강화

### 1.3 타겟 사용자
- 본인(개인 사용) — 대학원 논문 리뷰 및 학습 자료 정리용

---

## 2. 핵심 기능 정의 (두 앱 통합)

### 2.1 문서 뷰어 & 주석 (기본 레이어)
- PDF/EPUB 업로드 및 렌더링 (PDF.js 기반)
- 형광펜 하이라이트(색상별), 텍스트 주석, 자유 필기(태블릿/펜 입력 지원)
- 문서 내 검색, 목차(TOC) 자동 인식, 북마크

### 2.2 발췌-카드 통합 모델 (핵심 아키텍처)
MarginNote의 "카드 축(Card Axis)"과 LiquidText의 "발췌(Excerpt)" 개념을 통합한 단일 데이터 모델 설계:

| 통합 개념 | 설명 |
|---|---|
| **Node(노드)** | 하나의 발췌가 갖는 기본 단위. 원문 위치, 텍스트, 태그, 색상, 생성일 등 메타데이터 보유 |
| **뷰(View)** | 동일 Node를 문서 하이라이트 / 워크스페이스 카드 / 마인드맵 노드 / 플래시카드로 각각 렌더링 |
| **원본 추적(Source Link)** | 어느 뷰에서든 클릭 시 원문 위치로 즉시 이동 (LiquidText 방식 차용) |

### 2.3 자유 워크스페이스 (LiquidText 계열)
- 무한 캔버스에 발췌 카드를 자유 배치·그룹화
- 손그림 잉크 링크로 카드 간 관계 연결 (SVG 기반 벡터 라인)
- 프로젝트 내 다중 워크스페이스 생성, 이름/배경색 커스터마이즈
- 여러 문서를 나란히 열어 비교 보기 (split-pane, 최대 3분할)
- 하이라이트만 모아보는 압축 뷰(HighlightView 유사 기능)

### 2.4 마인드맵 & 구조화 (MarginNote 계열)
- 발췌 카드를 트리형 마인드맵으로 자동/수동 배치
- 문서 목차 기반 자동 마인드맵 생성
- 여러 문서의 노드를 하나의 마인드맵에 통합
- 개념 재등장 시 과거 주석 자동 연결(Title Link 유사 기능)

### 2.5 간격 반복 학습 (MarginNote 계열)
- 발췌 카드 → 플래시카드 자동 변환
- FSRS(Free Spaced Repetition Scheduler) 알고리즘 기반 복습 스케줄링
- 학습 진도 대시보드, 복습 알림 (로컬 알림 또는 앱 실행 시 표시)

### 2.6 연구 워크플로우 지원 (선택 기능)
- 로컬 파일로 관리 중인 Zotero 라이브러리(BibTeX/CSL-JSON export) 불러오기
- 발췌문 클릭 시 자동 인용 포맷 생성 (APA/MLA/Chicago 등)

### 2.7 내보내기 (백업 목적 포함)
- Markdown/PDF로 노트 내보내기
- 마인드맵을 이미지/OPML로 내보내기 (타 마인드맵 툴 호환)
- 플래시카드 Anki 포맷(.apkg) 내보내기
- **전체 데이터 백업/복원**: 로컬 DB를 단일 파일(zip 등)로 내보내고 다시 불러오는 기능 — 클라우드 없이도 기기 이전이나 백업이 가능하도록 필수로 포함

---

## 3. 기능 우선순위 (개발 단계 구분)

### Phase 1 — MVP (핵심 리딩 + 발췌)
- PDF 업로드/뷰어, 하이라이트, 텍스트 주석
- 발췌 → 워크스페이스 카드 생성 (드래그 앤 드롭)
- 워크스페이스 자유 배치, 원본 위치 추적
- OPFS 기반 로컬 저장 문서/폴더 관리
- **전체 데이터 백업/복원(zip export-import) 기본 기능** — 안드로이드 저장공간 관리 특성상 조기 구현
- PC 브라우저 + 갤럭시탭(Chrome/삼성 인터넷) 양쪽에서 기본 동작 검증

### Phase 2 — 구조화 기능
- 발췌 카드 → 마인드맵 노드 자동 생성
- 목차 기반 자동 마인드맵
- 다중 문서 비교 뷰 (split-pane)
- 잉크 링크(관계 연결선) 기능

### Phase 3 — 학습 기능
- 플래시카드 자동 생성 + FSRS 간격 반복
- 복습 대시보드
- Anki 내보내기

### Phase 4 — 워크플로우 보강 & 플랫폼 다듬기
- BibTeX/CSL-JSON 불러오기, 자동 인용 생성
- PWA 홈 화면 설치 최적화(갤럭시탭), 태블릿 화면 크기 대응 UI 조정
- S펜 필기 압력감지 세부 튜닝
- UI/성능 다듬기 (대용량 문서 기준 PC·태블릿 양쪽 성능 점검)

---

## 4. 기술 스택 (제안 — 로컬 우선 구조)

| 영역 | 기술 후보 | 비고 |
|---|---|---|
| 프론트엔드 | React + TypeScript | 컴포넌트 재사용성, 생태계 |
| PDF 렌더링 | PDF.js | 오픈소스, 커스터마이징 자유도 높음 |
| 캔버스/워크스페이스 | Konva.js 또는 React Flow | 무한 캔버스, 드래그앤드롭, 라인 연결에 적합 |
| 마인드맵 렌더링 | React Flow 또는 자체 구현 | 트리 레이아웃 + 자유 배치 겸용 |
| 상태 관리 | Zustand | 가볍고 카드-노드 동기화 로직에 적합 |
| **로컬 데이터 저장** | **IndexedDB (Dexie.js)** 또는 **SQLite (via sql.js/wa-sqlite, OPFS 백엔드)** | 발췌 카드·마인드맵·플래시카드 등 구조화 데이터 저장. IndexedDB는 PC·안드로이드 브라우저 모두 완전 지원 |
| **원본 파일 저장** | **OPFS(Origin Private File System) 단독 사용** | PDF 원본을 브라우저 전용 샌드박스 저장소에 보관. PC·Mac·안드로이드(Chrome/Samsung Internet 등) 전반에서 지원되어 크로스 플랫폼 기준으로 채택. ⚠️ File System Access API의 "폴더 직접 선택" 방식은 안드로이드 Chrome·삼성 인터넷에서 지원하지 않으므로 사용하지 않음 |
| 간격반복 알고리즘 | FSRS 오픈소스 라이브러리 | 이미 검증된 알고리즘 활용, 클라이언트에서 직접 연산 |
| 실행 형태 | **웹앱 + PWA(홈 화면 설치)** | PC에서는 브라우저로, 갤럭시탭에서는 PWA로 설치해 앱처럼 아이콘 실행. 추후 필요 시 PC용 Tauri/Electron 데스크톱 래핑도 검토 가능(안드로이드는 웹/PWA로 통일) |
| 필기 입력 | Pointer Events API (압력 감지) | S펜 등 스타일러스 압력값 활용, 갤럭시탭에서도 자연스러운 필기감 확보 |
| 백엔드 | **불필요** — 완전 클라이언트 사이드 | 서버 없이 브라우저 안에서 모든 저장·연산 수행. 안드로이드에서도 별도 로컬 서버 없이 동일하게 동작 |

> **설계 방향**: 서버·계정·클라우드 없이 브라우저(PC 또는 안드로이드 태블릿) 안에서 모든 것이 동작하는 **로컬 퍼스트(local-first) + 크로스 플랫폼** 구조. 저장소를 OPFS로 통일함으로써 PC/갤럭시탭 어느 쪽에서 써도 동일한 코드베이스로 동작한다.
>
> **참고**: OPFS에 저장된 파일은 기기의 "내 파일" 앱 등 일반 파일 탐색기에서는 보이지 않는 브라우저 전용 저장소다. 원본 파일을 기기 저장소로 직접 꺼내려면 앱 내 "내보내기" 기능을 통해야 하며, 이 때문에 백업/복원 기능의 중요도가 특히 높다(6장, 7장 참조).

---

## 5. 데이터 모델 개요 (초안)

```
Project (연구 프로젝트/과목 단위 — 로컬 폴더 개념)
 ├─ Document (PDF/EPUB 원본 — 로컬 파일 참조 또는 OPFS 저장)
 │    └─ Highlight (위치 좌표, 색상, 원문 텍스트)
 ├─ Node (발췌 카드 - 통합 개념)
 │    ├─ sourceHighlightId (원본 하이라이트 참조)
 │    ├─ tags[], color, memo
 │    └─ ViewInstances: [WorkspaceCardPosition, MindMapNodePosition, FlashcardMeta]
 ├─ Workspace (자유 캔버스, 프로젝트 내 다중 생성 가능)
 │    └─ CardPlacement (Node 참조 + x,y 좌표), InkLink (Node간 연결선)
 ├─ MindMap (트리 구조)
 │    └─ MindMapNode (Node 참조 + parent/child 관계)
 └─ FlashcardDeck
      └─ Flashcard (Node 참조 + FSRS 스케줄 데이터)
```

> 핵심 설계 원칙: **Node(발췌)는 한 번만 존재하고, 각 화면(워크스페이스/마인드맵/플래시카드)은 이를 참조하는 "뷰"로만 동작**한다. 한 곳에서 수정하면 모든 뷰에 반영되어야 함 (MarginNote 카드 축 구조 재현). 모든 데이터는 IndexedDB/OPFS에 저장되며 외부로 전송되지 않는다.

---

## 6. 리스크 및 고려사항

| 리스크 | 대응 방안 |
|---|---|
| 무한 캔버스 + 대용량 PDF 성능 이슈 | 가상 스크롤, 캔버스 레이어 최적화, PDF 페이지 지연 로딩 |
| 노드-뷰 동기화 복잡도 (한 곳 수정 시 전체 반영) | 단일 진실 공급원(Single Source of Truth) 설계, 이벤트 기반 동기화 |
| 브라우저 로컬 저장소 용량 제한 | OPFS 용량은 기기/브라우저별로 상이(안드로이드는 특히 저장공간 부족 시 자동 정리 위험 높음). 저장 용량 모니터링 UI 제공 검토 |
| **브라우저 데이터 삭제·저장공간 부족 시 유실 위험 (안드로이드에서 특히 중요)** | OPFS는 사용자 파일 관리 앱에서 보이지 않는 샌드박스 저장소이며 안드로이드는 저장공간 부족 시 정리될 가능성이 PC보다 높음. **zip 백업/복원 기능을 Phase 1 단계로 우선순위 상향**, 정기 백업 알림 UI 필수 적용 |
| 웹 기반 필기(손글씨) 입력 품질 | Pointer Events API + 압력감지(S펜 등 스타일러스) 대응, 벡터 기반 렌더링. 갤럭시탭 S펜 테스트 별도 진행 |
| PDF 표준 주석 포맷 미호환 이슈 (마진노트 사례) | PDF 표준 어노테이션 포맷(Annotation) 준수하여 타 앱과 호환성 확보 |
| **PC와 갤럭시탭 간 데이터 동기화 부재** | 클라우드를 쓰지 않기로 했으므로 기기 간 데이터는 자동 연동되지 않음. 필요 시 zip 백업 파일을 수동으로 옮겨 복원하는 방식으로 대체(자동 동기화 기능은 범위에서 제외) |

---

## 7. 개발 로드맵 (예시 — 개인 프로젝트 기준, 총 3~5개월)

| 기간 | 마일스톤 |
|---|---|
| 2~3주 | 기획 확정, 데이터 모델 설계, PDF 뷰어 + 하이라이트 구현 착수 (PC·갤럭시탭 브라우저 동시 개발환경 구성) |
| 1~1.5개월 | 발췌-워크스페이스 카드 연동, 원본 추적, OPFS/IndexedDB 저장 + 백업·복원 기능 완성 (Phase 1) |
| 2~2.5개월 | 마인드맵 자동 생성, 다중 문서 비교 뷰, 잉크 링크 (Phase 2) |
| 3~3.5개월 | 플래시카드 + FSRS 간격 반복 구현 (Phase 3) |
| 4~5개월 | BibTeX 불러오기, PWA/태블릿 UI 최적화, S펜 튜닝, 실사용하며 다듬기 (Phase 4) |

> 개인 프로젝트 특성상 일정은 참고용이며, 실제 사용하면서 필요한 기능부터 우선순위를 유연하게 조정 권장.

---

## 8. 다음 단계 (Action Items)

1. 실제 본인의 논문 리딩 워크플로우 정리 — 어떤 기능을 가장 먼저 쓸지 우선순위 확정
2. Node-View 통합 데이터 모델 상세 설계 문서화
3. PDF.js + IndexedDB 기반 최소 프로토타입 제작 (업로드 → 하이라이트 → 로컬 저장까지)
4. 캔버스 라이브러리 PoC (React Flow vs Konva.js 성능/사용성 비교) — 갤럭시탭에서도 렌더링 성능 함께 확인
5. 데이터 백업/복원(zip export-import) 방식 조기 설계 — 로컬 전용 앱이며 안드로이드는 저장공간 정리 위험이 있어 데이터 유실 방지가 특히 중요
6. 갤럭시탭 실기기에서 OPFS 저장 용량, S펜 필기 입력, PWA 설치 동작 조기 테스트 (Phase 1 단계에서 병행 권장)
